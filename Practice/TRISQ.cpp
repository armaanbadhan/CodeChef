#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t, b;
    cin >> t;
    for(int it = 0; it < t;) {
        cin >> b;
        int ans = 0;
        
        cout << ans << "\n";
        it++;
    }
    return 0;
}