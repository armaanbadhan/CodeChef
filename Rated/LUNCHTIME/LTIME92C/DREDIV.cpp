#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t, n, k;
    cin >> t;
    for(int it = 0; it < t; it++) {
        cin >> n >> k;
        for(int in = 0; in < n;) {
            
            in++;
        }
    }
    return 0;
}